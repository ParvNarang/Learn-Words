import 'package:flutter/material.dart';

final bgColor = Colors.indigo[600];
const white = Colors.white;
final btnStyle = ButtonStyle(
  backgroundColor: MaterialStateProperty.all<Color>(Colors.grey[300]!),
);